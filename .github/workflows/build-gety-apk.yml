name: Build Gety APK

on:
  push:
    branches: [ main ]
    paths:
      - 'gety/**'
      - '.github/workflows/build-gety-apk.yml'
  pull_request:
    paths:
      - 'gety/**'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: gety

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build debug APK
        run: gradle assembleDebug --no-daemon

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: gety-debug-apk
          path: gety/app/build/outputs/apk/debug/*.apk

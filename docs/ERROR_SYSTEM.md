# ERROR_SYSTEM.md — نظام التشخيص: البنية الداخلية + جدول الأكواد الكامل

> `app/src/main/cpp/diagnostics/diagnostic.h` نفسه يشير إلى هذا الملف كـ"جدول
> كامل بكل الأكواد" — هذا هو ذلك الجدول، مبنيّ مباشرة على `enum class Code` فيه.

## البنية: `Diagnostic` مقابل `RinError`

كل مرحلة معالجة (Lexer/Parser/Interpreter، وطبقة قاعدة البيانات المدمجة:
schema/index/relation/transaction...) تُصدر أخطاءها بإحدى طريقتين:

- **`Diagnostic`** (عبر `DiagnosticEngine::emit(d)`) — لخطأ يمكن **الاستمرار
  بأمان** بعده (error recovery). يُجمَّع في قائمة (`DiagnosticEngine::all()`)
  بدل التوقّف فورًا، بحيث يمكن الإبلاغ عن عدّة أخطاء دفعة واحدة من تشغيل واحد
  (مثل مُجمِّعات اللغات الحديثة) بدل التوقّف عند أول خطأ فقط.
- **`RinError`** (استثناء C++ عادي يحمل `Diagnostic` كامل) — لخطأ **لا يمكن**
  الاستمرار بأمان بعده (فشل قراءة ملف، أو حالة لا يجد فيها المحلِّل نقطة
  synchronization) — يُرفَع فورًا ويوقف التنفيذ.

`try`/`catch` على مستوى لغة Rin (انظر [`errors.md`](./errors.md)) يلتقط تحديدًا
هذه الأخطاء غير القابلة للاستمرار (`RinError`) أثناء التنفيذ، محوَّلة إلى قاموس
(map) بحقول `code`/`message`/`line`.

## `Severity`
كل `Diagnostic` له مستوى خطورة واحد من أربعة: `Error`, `Warning`, `Note`, `Help`
(`Note`/`Help` يُستخدَمان للسياق الإضافي المرفَق بخطأ/تحذير رئيسي، وليس ككيان
قائم بذاته — تمامًا كقسمَي `reason`/`help` في مثال الرسالة الغنيّة في
[`errors.md`](./errors.md#رسائل-الخطأ-الغنيّة-rich-diagnostics)).

## جدول الأكواد الكامل (`diag::Code`)

| كود | المعنى |
|---|---|
| E0001 | متغيّر غير معرَّف (Undefined Variable) |
| E0002 | متغيّر مكرَّر (Duplicate Variable) |
| E0003 | إسناد غير صالح |
| E0004 | نوع غير صالح لعامل/عملية |
| E0005 | عدم تطابق أنواع |
| E0006 | دالة غير معروفة |
| E0007 | وسائط غير صالحة |
| E0008 | `return` غير صالح |
| E0009 | `return` مفقود |
| E0010 | خطأ تحليل نحوي عام |
| E0011 | توكن غير متوقَّع |
| E0012 | توكن مفقود |
| E0013 | تعبير غير صالح |
| E0014 | حاوية غير صالحة |
| E0015 | حاوية غير معروفة |
| E0016 | خاصية غير صالحة (يشمل أخطاء سياسة Make Unit، انظر [`MAKE_UNIT.md`](./MAKE_UNIT.md)) |
| E0017 | خاصية غير معرَّفة |
| E0018 | مخطَّط (Schema) غير صالح |
| E0019 | انتهاك مخطَّط |
| E0020 | مستند غير صالح |
| E0021 | فهرس غير صالح (خارج المدى) |
| E0022 | علاقة غير صالحة |
| E0023 | خطأ معاملة (Transaction) |
| E0024 | خطأ ترحيل (Migration) |
| E0025 | خطأ ذاكرة تخزين مؤقت |
| E0026 | خطأ غير متزامن (Async) |
| E0027 | `await` خارج سياق `async` |
| E0028 | خطأ استيراد |
| E0029 | وحدة غير موجودة |
| E0030 | اعتمادية دائرية |
| E0031 | خطأ عام (Generic) |
| E0032 | وسيط generic غير صالح |
| E0033 | خطأ ملكية (Ownership) |
| E0034 | خطأ استعارة (Borrow) |
| E0035 | خطأ تشغيل عام |
| E0036 | فشل إدخال/إخراج |
| E0037 | خطأ شبكة |
| E0038 | خطأ حزمة |
| E0039 | خطأ داخلي في المترجم |
| E0040 | ميزة غير مدعومة |
| E0041 | اعتمادية مفقودة (`requires X;` حيث X غير موجودة في الشجرة وقت الفحص) |
| E0042 | خطأ تركيبي في استعلام RCSQL، أو رمز خارج القائمة المسموحة |
| W0001 | متغيّر غير مستخدَم |
| W0002 | استيراد غير مستخدَم |
| W0003 | كود غير قابل للوصول |
| W0004 | ميزة مهجورة (Deprecated) |
| W0005 | تظليل متغيّر (Shadowed) |
| W0006 | تحويل غير ضروري |
| W0007 | دالة غير مستخدَمة |
| W0008 | مقارنة مريبة |

## انظر أيضًا
- [`errors.md`](./errors.md) — الاستخدام الفعلي: `try`/`catch`/`throw` وشكل
  الرسالة الغنيّة، بأمثلة حقيقية.
- `app/src/main/cpp/diagnostics/` — التنفيذ الكامل (`diagnostic.h`/`.cpp`،
  `diagnostic_engine.h`/`.cpp`، `diagnostic_renderer.h`/`.cpp`، `source_manager.h`/`.cpp`).

<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.RinStudioAuth"
        android:supportsRtl="true">

        <activity
            android:name="com.dlof.rinlang.auth.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.dlof.rinlang.auth.RegisterActivity"
            android:exported="false" />

        <activity
            android:name="com.dlof.rinlang.auth.VerifyCodeActivity"
            android:exported="false" />

        <activity
            android:name="com.dlof.rinlang.auth.DevicePairingActivity"
            android:exported="false" />

    </application>

</manifest>

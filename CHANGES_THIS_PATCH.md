# التعديلات في هذا الأرشيف

انسخ هذه الملفات فوق نظيراتها في مشروعك (نفس المسارات بالضبط).

## 1) إصلاح مشكلة الحذف/الكتابة في المحرر
- **الملف:** `app/src/main/java/com/dlof/rinlang/RinCodeEditorView.kt`
- **السبب الجذري:** معالجة تعديلات لوحة المفاتيح (IME) كانت تصير داخل `onTextChanged`،
  وهو يُستدعى من *منتصف* عملية تعديل النص الأصلية غير المكتملة بعد على نفس الكائن
  (Editable). أي كتابة إضافية على نفس الكائن في هذه اللحظة (وهو ما كان يصير: استدعاء
  محرك Rin ثم إعادة كتابة النص بالكامل) هي تعديل متداخل (reentrant) غير آمن — وهذا بالضبط
  ما كان يسبب: حذف حرف زيادة أو نقصان، كتابة تتكرر/حروف تنقلب، وتجمّد التطبيق أحياناً.
- **الحل:** نقل التنفيذ الفعلي إلى `afterTextChanged` (الطريقة القياسية بأندرويد)، مع
  الاكتفاء في `onTextChanged` بالتقاط بيانات التغيير فقط (قراءة بلا أي كتابة).

## 2) تجديد شكل صفحة "المكتبات" وصفحة "حزمي المنشورة"
- **الملف:** `app/src/main/java/com/dlof/rinlang/LibrariesActivity.kt` — ربط رأس الشاشة
  الجديد باسم المشروع وعدد المكتبات فعلياً (بدل نص ثابت).
- **`res/layout/activity_libraries.xml`** — رأس جديد بتدرّج بنفسجي (أيقونة + اسم المشروع +
  حبّة عدد المكتبات) بدل عنوان نصي مسطّح.
- **`res/layout/item_library.xml`** — بطاقة بيضاء بشريط تمييز بنفسجي جانبي، أيقونة متدرّجة،
  ومعلومة meta على شكل حبّة رمادية بدل نص عائم.
- **`res/layout/item_builtin_library.xml`** — نفس اللغة البصرية لكن بشريط أخضر لتمييز
  المكتبات الجاهزة فوراً عن مكتبات المستخدم.
- **`res/layout/item_my_package.xml`** (شاشة الحساب، قسم "حزمي المنشورة") — نفس لغة
  البطاقات + شارة خضراء "منشورة".
- **درويابلز جديدة** (لا تمسّ أي ستايل مشترك تستخدمه شاشات أخرى):
  `bg_libraries_hero.xml`, `bg_hero_icon_ring.xml`, `bg_hero_stat_pill.xml`,
  `bg_library_card_mine.xml`, `bg_library_card_builtin.xml`, `bg_icon_gradient_green.xml`,
  `bg_chip_meta.xml`, `bg_chip_published.xml`.
- **`res/values/colors.xml`** — إضافة لون واحد جديد فقط: `rin_chip_bg`.
- **`res/values*/strings_store.xml`** (ar/en/es + الافتراضي) — إضافة نص شارة جديد:
  `package_published_badge`.

## ملفات محذوفة
لا يوجد أي ملف يجب حذفه في هذا التحديث.

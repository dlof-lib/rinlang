<?xml version="1.0" encoding="utf-8"?>
<!-- صف يفتح شاشة أخرى: أيقونة بدائرة خلفية + عنوان + تلميح + سهم. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="10dp"
    android:background="@drawable/bg_setting_row"
    android:clickable="true"
    android:focusable="true"
    android:gravity="center_vertical"
    android:padding="14dp">

    <TextView
        android:id="@+id/settingIcon"
        android:layout_width="46dp"
        android:layout_height="46dp"
        android:background="@drawable/bg_setting_icon_avatar"
        android:gravity="center"
        android:textSize="22sp" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:paddingStart="12dp">

        <TextView
            android:id="@+id/settingTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/rin_on_toolbar"
            android:textSize="14.5sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/settingHint"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="3dp"
            android:textColor="@color/rin_editor_hint"
            android:textSize="11.5sp" />
    </LinearLayout>

    <TextView
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:gravity="center"
        android:importantForAccessibility="no"
        android:text="›"
        android:textColor="@color/rin_accent"
        android:textSize="24sp"
        android:textStyle="bold" />
</LinearLayout>

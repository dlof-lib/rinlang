<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/rin_background">

    <include layout="@layout/toolbar_app" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingTop="14dp"
        android:paddingBottom="8dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical">

            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/projects_screen_heading"
                    android:textColor="@color/rin_on_toolbar"
                    android:textSize="22sp"
                    android:textStyle="bold" />

                <TextView
                    android:id="@+id/txtProjectCount"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="3dp"
                    android:text="@string/projects_count_zero"
                    android:textColor="@color/rin_editor_hint"
                    android:textSize="12.5sp" />
            </LinearLayout>

            <TextView
                android:id="@+id/btnProjectAlbums"
                android:layout_width="wrap_content"
                android:layout_height="42dp"
                android:minWidth="92dp"
                android:gravity="center"
                android:paddingStart="14dp"
                android:paddingEnd="14dp"
                android:text="@string/albums_title"
                android:textColor="@color/rin_accent"
                android:textSize="12.5sp"
                android:textStyle="bold"
                android:background="@drawable/bg_projects_secondary_button"
                android:clickable="true"
                android:focusable="true" />
        </LinearLayout>

        <EditText
            android:id="@+id/inputProjectSearch"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:layout_marginTop="12dp"
            android:background="@drawable/bg_projects_search"
            android:drawableStart="@android:drawable/ic_menu_search"
            android:drawableTint="@color/rin_editor_hint"
            android:hint="@string/search_projects_hint"
            android:paddingStart="14dp"
            android:paddingEnd="14dp"
            android:textColor="@color/rin_on_toolbar"
            android:textColorHint="@color/rin_editor_hint"
            android:textSize="13sp"
            android:singleLine="true"
            android:inputType="text"
            android:imeOptions="actionDone" />
    </LinearLayout>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvProjects"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:paddingTop="4dp"
            android:paddingBottom="92dp" />

        <LinearLayout
            android:id="@+id/txtEmptyProjects"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:orientation="vertical"
            android:gravity="center_horizontal"
            android:paddingStart="28dp"
            android:paddingEnd="28dp"
            android:visibility="gone">

            <ImageView
                android:layout_width="72dp"
                android:layout_height="72dp"
                android:src="@drawable/ic_empty_folder"
                android:alpha="0.85"
                android:contentDescription="@null" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="14dp"
                android:text="@string/no_projects_yet"
                android:textColor="@color/rin_on_toolbar"
                android:textSize="16sp"
                android:textStyle="bold"
                android:gravity="center" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="5dp"
                android:text="@string/no_projects_hint"
                android:textColor="@color/rin_editor_hint"
                android:textSize="12sp"
                android:gravity="center" />
        </LinearLayout>

        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fabNewProject"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|end"
            android:layout_margin="18dp"
            android:contentDescription="@string/new_project_title"
            app:backgroundTint="@color/rin_accent"
            app:tint="@android:color/white"
            app:rippleColor="@color/rin_accent_pressed"
            app:srcCompat="@android:drawable/ic_input_add" />
    </FrameLayout>

    <include layout="@layout/bottom_nav_bar" />
</LinearLayout>

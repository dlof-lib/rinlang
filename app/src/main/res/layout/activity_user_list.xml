<?xml version="1.0" encoding="utf-8"?>
<!-- شاشة قائمة مستخدمين عامة، تُستخدم لعرض "المنتسبين" (متابعو حساب ما) أو "الانتسابات"
     (الحسابات التي يتابعها حساب ما)، حسب UserListActivity.EXTRA_MODE الممرَّر إليها. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/rin_background">

    <include layout="@layout/toolbar_app" />

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:fillViewport="true">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="14dp">

            <TextView
                android:id="@+id/txtUserListEmpty"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="24dp"
                android:gravity="center"
                android:textColor="@color/rin_editor_hint"
                android:textSize="13.5sp"
                android:visibility="gone" />

            <LinearLayout
                android:id="@+id/containerUserList"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical" />

        </LinearLayout>
    </androidx.core.widget.NestedScrollView>

</LinearLayout>

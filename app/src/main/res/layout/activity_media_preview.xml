<?xml version="1.0" encoding="utf-8"?>
<!-- شاشة معاينة وسائط بسيطة: شريط علوي موحّد (يعرض اسم الملف) + صورة أو فيديو بملء الشاشة،
     تُفتَح عند الضغط على ملف صورة/فيديو داخل شاشة "الملفات" ([FilesActivity]). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@android:color/black">

    <include layout="@layout/toolbar_app" />

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="@android:color/black">

        <ImageView
            android:id="@+id/imgMediaPreview"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="fitCenter"
            android:visibility="gone"
            android:contentDescription="@null" />

        <VideoView
            android:id="@+id/videoMediaPreview"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center"
            android:visibility="gone" />

        <TextView
            android:id="@+id/txtMediaPreviewError"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_margin="32dp"
            android:gravity="center"
            android:text="@string/file_open_error"
            android:textColor="@color/rin_editor_hint"
            android:textSize="14sp"
            android:visibility="gone" />

    </FrameLayout>

</LinearLayout>
